class MtInfo{
  final String text, encoding, dlrUrl, validUntil;
  final int totalPart, totalChar;
  final bool dlrRequested;

  MtInfo(this.text, this.encoding, this.dlrUrl, this.validUntil, this.totalPart, this.totalChar, this.dlrRequested);
}